use super::NsConfig;
use super::NsError;
use super::NsContext;


pub struct Server {
    pub fn new(config: &NsConfig) -> NsResult<i32> {
        Ok(0)
    }

    pub fn use(ctx: &NsContext) -> NsResult<i32> {
        Ok(0)
    }

    pub fn run(addr: &str) -> NsResult<i32> {
        Ok(0)
    }
}
